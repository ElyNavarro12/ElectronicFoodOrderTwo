<!-- Encabezado para punto de ventas solo para sesiones administrativas !-->
<% if user_signed_in? %>

<header class="baseBox-backend">
  <!-- Titulo !-->
  <div class="title-backend baseBox-backend">
    <span class="icon-pacman baseBox-backend"></span>
    <h1>NoAppName</h1>
  </div>
  <!-- Opciones de navegacion !-->
  <div class="navBar-backend baseBox-backend">
    <!-- Configuraciones !-->
    <div class="config-backend">
      <%= link_to 'config', '/config' %>
    </div>
    <!-- Cerrar sesion actual !-->
    <div class="sign_out-backend baseBox-backend">
      <span class="icon-user"></span>
      <%= link_to current_user.name, destroy_user_session_path, method: :delete  %>
    </div>
  </div>
</header>

<!-- Encabezado para la aplicacion de los clientes !-->
<% else %>

<% end %>
